// Main entry point - All functionality moved to other modules.
// Ensure all required scripts (app_init.js, ui_helpers.js, etc.) are loaded in the HTML.

console.log("index.js loaded - primary initialization occurs in app_init.js");